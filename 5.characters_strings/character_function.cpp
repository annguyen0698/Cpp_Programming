/*
#include <<ccstype>>
function_name(char)
-functions for testing character 
-function for converting character case

=========================================================
Testing character
    -- isalpha(c) : True if c is a letter
    -- isalnum(c) : True if c is a number 
    -- isdigit(c) : True if c is a digit 
    -- islower(c) : True if c is in lower case
    -- isprint(c) : True if c is a printable character 
    -- ispunct(c) : True if c is a punctuation character 
    -- isupper(c) : True if c is an upper case letter 
    -- isspace(c) : True if c is a white space 
==========================================================
Converting character 
    -- to lower (c) : return lower case of c 
    -- to upper (c) : return upper case of c


*/